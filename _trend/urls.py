# -*- coding: utf-8 -*-
from django.conf.urls.defaults import *

urlpatterns = patterns('',
    #(r'^$', 'trend.views.index'),
    #(r'^signup/$', 'trend.views.create_new_user'),
    #(r'^login/$', 'django.contrib.auth.views.login', {'template_name': 'trend/user_create_form.html'}),
    #(r'^logout/$', 'django.contrib.auth.views.logout_then_login', {'login_url': '/trend/'}),
)
